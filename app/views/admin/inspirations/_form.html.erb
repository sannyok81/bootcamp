<%= form_with(model: inspiration, local: true) do |form| %>
  <% if inspiration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(inspiration.errors.count, "error") %> prohibited this inspiration from being saved:</h2>

      <ul>
      <% inspiration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link, id: :inspiration_link %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image, id: :inspiration_image %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :inspiration_title %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text, id: :inspiration_text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
